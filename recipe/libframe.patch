--- configure	2019-02-28 03:04:22.000000000 +0000
+++ configure	2019-05-22 15:38:01.331760641 +0100
@@ -786,8 +786,6 @@
 LAL_LIBS
 LAL_CFLAGS
 LAL_VERSION
-LIBFRAME_LIBS
-LIBFRAME_CFLAGS
 METAIO_LIBS
 METAIO_CFLAGS
 GSL_LIBS
@@ -1022,8 +1020,6 @@
 GSL_LIBS
 METAIO_CFLAGS
 METAIO_LIBS
-LIBFRAME_CFLAGS
-LIBFRAME_LIBS
 LAL_CFLAGS
 LAL_LIBS
 LAL_HTMLDIR
@@ -1753,10 +1749,6 @@
   METAIO_CFLAGS
               C compiler flags for METAIO, overriding pkg-config
   METAIO_LIBS linker flags for METAIO, overriding pkg-config
-  LIBFRAME_CFLAGS
-              C compiler flags for LIBFRAME, overriding pkg-config
-  LIBFRAME_LIBS
-              linker flags for LIBFRAME, overriding pkg-config
   LAL_CFLAGS  C compiler flags for LAL, overriding pkg-config
   LAL_LIBS    linker flags for LAL, overriding pkg-config
   LAL_HTMLDIR value of htmldir for lowercase >= ${MIN_LAL_VERSION}, overriding
@@ -24453,277 +24445,6 @@
 done
 
 
-# check for frame library
-
-pkg_failed=no
-{ $as_echo "$as_me:${as_lineno-$LINENO}: checking for LIBFRAME" >&5
-$as_echo_n "checking for LIBFRAME... " >&6; }
-
-if test -n "$LIBFRAME_CFLAGS"; then
-    pkg_cv_LIBFRAME_CFLAGS="$LIBFRAME_CFLAGS"
- elif test -n "$PKG_CONFIG"; then
-    if test -n "$PKG_CONFIG" && \
-    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"libframe\""; } >&5
-  ($PKG_CONFIG --exists --print-errors "libframe") 2>&5
-  ac_status=$?
-  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
-  test $ac_status = 0; }; then
-  pkg_cv_LIBFRAME_CFLAGS=`$PKG_CONFIG --cflags "libframe" 2>/dev/null`
-		      test "x$?" != "x0" && pkg_failed=yes
-else
-  pkg_failed=yes
-fi
- else
-    pkg_failed=untried
-fi
-if test -n "$LIBFRAME_LIBS"; then
-    pkg_cv_LIBFRAME_LIBS="$LIBFRAME_LIBS"
- elif test -n "$PKG_CONFIG"; then
-    if test -n "$PKG_CONFIG" && \
-    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"libframe\""; } >&5
-  ($PKG_CONFIG --exists --print-errors "libframe") 2>&5
-  ac_status=$?
-  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
-  test $ac_status = 0; }; then
-  pkg_cv_LIBFRAME_LIBS=`$PKG_CONFIG --libs "libframe" 2>/dev/null`
-		      test "x$?" != "x0" && pkg_failed=yes
-else
-  pkg_failed=yes
-fi
- else
-    pkg_failed=untried
-fi
-
-
-
-if test $pkg_failed = yes; then
-   	{ $as_echo "$as_me:${as_lineno-$LINENO}: result: no" >&5
-$as_echo "no" >&6; }
-
-if $PKG_CONFIG --atleast-pkgconfig-version 0.20; then
-        _pkg_short_errors_supported=yes
-else
-        _pkg_short_errors_supported=no
-fi
-        if test $_pkg_short_errors_supported = yes; then
-	        LIBFRAME_PKG_ERRORS=`$PKG_CONFIG --short-errors --print-errors --cflags --libs "libframe" 2>&1`
-        else
-	        LIBFRAME_PKG_ERRORS=`$PKG_CONFIG --print-errors --cflags --libs "libframe" 2>&1`
-        fi
-	# Put the nasty error message in config.log where it belongs
-	echo "$LIBFRAME_PKG_ERRORS" >&5
-
-	false
-elif test $pkg_failed = untried; then
-     	{ $as_echo "$as_me:${as_lineno-$LINENO}: result: no" >&5
-$as_echo "no" >&6; }
-	false
-else
-	LIBFRAME_CFLAGS=$pkg_cv_LIBFRAME_CFLAGS
-	LIBFRAME_LIBS=$pkg_cv_LIBFRAME_LIBS
-        { $as_echo "$as_me:${as_lineno-$LINENO}: result: yes" >&5
-$as_echo "yes" >&6; }
-	true
-fi
-
-  # LALSUITE_ADD_FLAGS: prepend/append flags to AM_CPPFLAGS/AM_CFLAGS/AM_LDFLAGS/LIBS,
-  # and update values of CPPFLAGS/CFLAGS/LDFLAGS for Autoconf tests
-  # - arg 1: prefix of the compiler flag variable, e.g. C for CFLAGS, CXX for CXXFLAGS
-  # - arg 2: compiler flags
-  # - arg 3: linker flags
-
-    pre_AM_CPPFLAGS=
-    post_AM_CPPFLAGS=
-    pre_sys_CPPFLAGS=
-    post_AM_CFLAGS=
-    for flag in ${LIBFRAME_CFLAGS}; do
-      # AM_CPPFLAGS gets unique -I, -D and -U flags
-      # sys_CPPFLAGS gets unique system -I flags
-      # AM_CFLAGS gets everything else
-      case ${flag} in #(
-  -I/opt/*|-I/usr/*) :
-
-          case " ${sys_CPPFLAGS} " in #(
-  *" ${flag} "*) :
-    : ;; #(
-  *) :
-    pre_sys_CPPFLAGS="${pre_sys_CPPFLAGS} ${flag}"
-           ;;
-esac
-         ;; #(
-  -I*) :
-
-          case " ${AM_CPPFLAGS} " in #(
-  *" ${flag} "*) :
-    : ;; #(
-  *) :
-    pre_AM_CPPFLAGS="${pre_AM_CPPFLAGS} ${flag}"
-           ;;
-esac
-         ;; #(
-  -D*|-U*) :
-    post_AM_CPPFLAGS="${post_AM_CPPFLAGS} ${flag}" ;; #(
-  *) :
-    post_AM_CFLAGS="${post_AM_CFLAGS} ${flag}"
-       ;;
-esac
-    done
-    if test "x${pre_AM_CPPFLAGS}" != x; then :
-
-      AM_CPPFLAGS="${pre_AM_CPPFLAGS} ${AM_CPPFLAGS}"
-      $as_echo "$as_me:${as_lineno-$LINENO}: prepended ${pre_AM_CPPFLAGS} to AM_CPPFLAGS" >&5
-
-fi
-    if test "x${post_AM_CPPFLAGS}" != x; then :
-
-      AM_CPPFLAGS="${AM_CPPFLAGS} ${post_AM_CPPFLAGS}"
-      $as_echo "$as_me:${as_lineno-$LINENO}: appended ${post_AM_CPPFLAGS} to AM_CPPFLAGS" >&5
-
-fi
-    if test "x${pre_sys_CPPFLAGS}" != x; then :
-
-      sys_CPPFLAGS="${pre_sys_CPPFLAGS} ${sys_CPPFLAGS}"
-      $as_echo "$as_me:${as_lineno-$LINENO}: prepended ${pre_sys_CPPFLAGS} to system AM_CPPFLAGS" >&5
-
-fi
-    if test "x${post_AM_CFLAGS}" != x; then :
-
-      AM_CFLAGS="${AM_CFLAGS} ${post_AM_CFLAGS}"
-      $as_echo "$as_me:${as_lineno-$LINENO}: appended ${post_AM_CFLAGS} to AM_CFLAGS" >&5
-
-fi
-    CPPFLAGS="${AM_CPPFLAGS} ${sys_CPPFLAGS} ${lalsuite_uvar_CPPFLAGS}"
-    CFLAGS="${AM_CFLAGS} ${lalsuite_uvar_CFLAGS}"
-
-
-    pre_AM_LDFLAGS=
-    post_AM_LDFLAGS=
-    pre_sys_LDFLAGS=
-    pre_LIBS=
-    for flag in ${LIBFRAME_LIBS}; do
-      # LIBS gets -l flags and .la files
-      # sys_LDFLAGS gets unique system -L flags
-      # AM_LDFLAGS gets unique -L flags and everything else
-      case ${flag} in #(
-  -L/opt/*|-L/usr/*) :
-
-          case " ${sys_LDFLAGS} " in #(
-  *" ${flag} "*) :
-    : ;; #(
-  *) :
-    pre_sys_LDFLAGS="${pre_sys_LDFLAGS} ${flag}"
-           ;;
-esac
-         ;; #(
-  -L*) :
-
-          case " ${AM_LDFLAGS} " in #(
-  *" ${flag} "*) :
-    : ;; #(
-  *) :
-    pre_AM_LDFLAGS="${pre_AM_LDFLAGS} ${flag}"
-           ;;
-esac
-         ;; #(
-  -l*|*.la) :
-    pre_LIBS="${pre_LIBS} ${flag}" ;; #(
-  *) :
-    post_AM_LDFLAGS="${post_AM_LDFLAGS} ${flag}"
-       ;;
-esac
-    done
-    if test "x${pre_AM_LDFLAGS}" != x; then :
-
-      AM_LDFLAGS="${pre_AM_LDFLAGS} ${AM_LDFLAGS}"
-      $as_echo "$as_me:${as_lineno-$LINENO}: prepended ${pre_AM_LDFLAGS} to AM_LDFLAGS" >&5
-
-fi
-    if test "x${post_AM_LDFLAGS}" != x; then :
-
-      AM_LDFLAGS="${AM_LDFLAGS} ${post_AM_LDFLAGS}"
-      $as_echo "$as_me:${as_lineno-$LINENO}: appended ${post_AM_LDFLAGS} to AM_LDFLAGS" >&5
-
-fi
-    if test "x${pre_sys_LDFLAGS}" != x; then :
-
-      sys_LDFLAGS="${pre_sys_LDFLAGS} ${sys_LDFLAGS}"
-      $as_echo "$as_me:${as_lineno-$LINENO}: prepended ${pre_sys_LDFLAGS} to system AM_LDFLAGS" >&5
-
-fi
-    if test "x${pre_LIBS}" != x; then :
-
-      LIBS="${pre_LIBS} ${LIBS}"
-      $as_echo "$as_me:${as_lineno-$LINENO}: prepended ${pre_LIBS} to LIBS" >&5
-
-fi
-    LDFLAGS="${AM_LDFLAGS} ${sys_LDFLAGS} ${lalsuite_uvar_LDFLAGS}"
-
-  # end LALSUITE_ADD_FLAGS
-
-{ $as_echo "$as_me:${as_lineno-$LINENO}: checking for FrLibIni in -lFrame" >&5
-$as_echo_n "checking for FrLibIni in -lFrame... " >&6; }
-if ${ac_cv_lib_Frame_FrLibIni+:} false; then :
-  $as_echo_n "(cached) " >&6
-else
-  ac_check_lib_save_LIBS=$LIBS
-LIBS="-lFrame  $LIBS"
-cat confdefs.h - <<_ACEOF >conftest.$ac_ext
-/* end confdefs.h.  */
-
-/* Override any GCC internal prototype to avoid an error.
-   Use char because int might match the return type of a GCC
-   builtin and then its argument prototype would still apply.  */
-#ifdef __cplusplus
-extern "C"
-#endif
-char FrLibIni ();
-int
-main ()
-{
-return FrLibIni ();
-  ;
-  return 0;
-}
-_ACEOF
-if ac_fn_c_try_link "$LINENO"; then :
-  ac_cv_lib_Frame_FrLibIni=yes
-else
-  ac_cv_lib_Frame_FrLibIni=no
-fi
-rm -f core conftest.err conftest.$ac_objext \
-    conftest$ac_exeext conftest.$ac_ext
-LIBS=$ac_check_lib_save_LIBS
-fi
-{ $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_cv_lib_Frame_FrLibIni" >&5
-$as_echo "$ac_cv_lib_Frame_FrLibIni" >&6; }
-if test "x$ac_cv_lib_Frame_FrLibIni" = xyes; then :
-  cat >>confdefs.h <<_ACEOF
-#define HAVE_LIBFRAME 1
-_ACEOF
-
-  LIBS="-lFrame $LIBS"
-
-else
-  as_fn_error $? "could not find the frame library" "$LINENO" 5
-fi
-
-
-# check for frame library headers
-for ac_header in FrameL.h
-do :
-  ac_fn_c_check_header_mongrel "$LINENO" "FrameL.h" "ac_cv_header_FrameL_h" "$ac_includes_default"
-if test "x$ac_cv_header_FrameL_h" = xyes; then :
-  cat >>confdefs.h <<_ACEOF
-#define HAVE_FRAMEL_H 1
-_ACEOF
-
-else
-  as_fn_error $? "could not find the FrameL.h header" "$LINENO" 5
-fi
-
-done
-
-
 # check for lal libraries and headers
 
   # LALSUITE_CHECK_LIB: check for LAL library
